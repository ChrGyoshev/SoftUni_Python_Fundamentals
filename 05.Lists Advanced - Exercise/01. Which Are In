string_a = input().split(", ")
string_b = input().split(", ")

my_list = [ subst for subst in string_a for word in string_b if subst in word]
final = sorted(set(my_list), key=my_list.index)
print(final)
